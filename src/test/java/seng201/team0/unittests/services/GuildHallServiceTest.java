/*package seng201.team0.unittests.services;

import javafx.scene.control.ListView;
import javafx.stage.Stage;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import seng201.team0.GameEnvironment;
import seng201.team0.gui.ScreenNavigator;
import seng201.team0.models.Adventurer;
import seng201.team0.services.GuildHallService;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class GuildHallServiceTest {
    GuildHallService guildHallService;
    ListView<Adventurer> listView = new ListView<>();

    @Test
    public void testFillNewAdventurerListCorrectSize() {
        guildHallService.fillNewAdventurerList(listView, 5);
        assertEquals(5, listView.getItems().size());
        assertEquals(5, guildHallService.getAdventurerList().size());
    }
}*/

// i cant figure out how to get it working with the gameEnvironment
